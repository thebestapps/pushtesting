import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Onesignal_App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
